<% @categories.each do |category| %>
  <%= link_to category.title, category_path(category) %>
<% end %>

<% @items.each do |item| %>
  <% if item.inventory > 0 %>
    <p><%= render partial: "items/item", locals: {item: item} %></p>
  <% end %>
<% end %>
